# Git repository details
A React application to work with the Git apis to get a git repository lastest commits and stars using Node-Js as back-end

### Installation & Run

on the root of the application run the following commands
- Run **`yarn`** to install the `concurrently` package.
- Run **`npm run setup`** to install the packages for the both server side and client side applications.
- Run **`npm start`** to start the both application.

### Screen shots
##### git get info screen
<img src="https://github.com/mo0rti/harver/blob/master/screenshots/entry_form.png" width="400" aspectRatio="1/2">

##### git repo details screen
<img src="https://github.com/mo0rti/harver/blob/master/screenshots/result.png" width="400" aspectRatio="1/2">